# asana-board-mark-old-cards

Highlights cards in a specified section that are older than specified number of days.

## Usage
 - Replace `'<auth-token-here>'` in `dist/index.js` with your auth token 
 - Use it as a bookmarklet
